var minhaVar = 'minha variavel';

function minhaFunc(x,y)
{
  return x+y;
}
//es6
let num = 2;
const PI =3.14;

var numeros = [1,2,3];
numeros.map(function(valor) {
  return valor * 2;
})

numeros.map(valor => {
 valor * 2;
})
 //ou

 numeros.map(valor => valor * 2) //es6
 //transpieler ex: babel transpiler ec6 em vanilla js

 class Matematica {
   soma (x,y)
   {
     return x+y;
   }
 }
var n1: string = 'sdssd'; //tipar variaveis
n1 =4;
